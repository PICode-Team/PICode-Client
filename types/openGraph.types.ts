export interface IOpenGraph {
  article?: { author: string }
  description?: string
  image?: { url: string }
  regDate?: string
  site_name?: string
  title?: string
  type?: string
  url?: string
}
