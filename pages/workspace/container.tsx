import React from 'react'

import Layout from '../../components/layout'
import DefaultContainer from '../../components/service/workpspace/container'

export default function CodePages(pageProps: any) {
  return (
    <Layout {...pageProps}>
      <DefaultContainer />
    </Layout>
  )
}
